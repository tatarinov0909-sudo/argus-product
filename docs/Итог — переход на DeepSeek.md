# Итог ночной сессии — Оркестратор переведён на DeepSeek

Сделано полностью, задеплоено на прод, проверено живьём.

## Что изменилось

- Оркестратор (`/api/agents/orchestrator/ask`) теперь работает на **DeepSeek v4-flash**,
  не на Claude — по результатам сравнения на 13 тестах оба справились одинаково
  (13/13), DeepSeek в ~4 раза дешевле и, главное, **реально доступен с сервера
  в России** (Claude API возвращал `403 forbidden` — вероятно, экспортные
  ограничения США в отношении России, не техническая мелочь).
- Claude-версия (`src/agents/orchestrator.js`) осталась в коде нетронутой —
  на случай, если понадобится сравнить снова или вернуться. Просто не подключена
  к роуту сейчас.
- Общий промпт и описание инструмента вынесены в `orchestratorPrompt.js` —
  используются обеими версиями, чтобы сравнение оставалось честным.

## Задеплоено на прод (193.187.94.167)

- `DEEPSEEK_API_KEY` добавлен в `.env` на сервере
- `src/agents/routes.js`, `orchestrator.js`, `orchestratorDeepseek.js`,
  `orchestratorPrompt.js` — залиты
- `package.json`/`package-lock.json` обновлены (добавлен `@anthropic-ai/sdk`,
  он же нужен для Claude-версии, которая осталась в коде)
- `pm2 restart` выполнен, сервис стабилен (проверено: `/health` → `200 OK`,
  без падений после рестарта)

## Проверено живьём в проде

Зарегистрирован одноразовый тестовый владелец, задан вопрос через
`/api/agents/orchestrator/ask` про несуществующий артикул — ответ корректный
(«ничего не нашёл», без выдумки). Тестовый аккаунт удалён из базы сразу после
проверки — прод чист.

## Не сделано и не тронуто

- **1С ни разу не трогали.** Ложная тревога про «сломалась 1С» была проверена —
  логи сервера чистые, `/health` в порядке, физически наш VPS не может повлиять
  на сервер клиента (`192.168.137.92`) — разные машины, разные сети, только
  1С сама стучится к нам, не наоборот.
- Git-коммит не делал — изменения лежат в рабочем дереве незакоммиченными.
  Закоммитить, если попросите явно.
- Вопрос легального доступа к Anthropic API из России (экспортные
  ограничения) остался открытым и нерешённым — просто перешли на DeepSeek,
  который эту проблему не имеет. Если когда-нибудь понадобится вернуться
  к Claude в проде — сначала решить этот вопрос с юристом, не в одиночку.

## Что дальше (не начато, ждёт вашего решения)

Порядок из более раннего плана: подсказка ячейки при приёмке (правила,
без LLM) — следующий кандидат, независимо от того, на чём Оркестратор.
